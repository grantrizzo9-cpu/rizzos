'use client';

import { type ReactNode } from "react";

// This file is a placeholder. PayPal functionality has been removed.
export function PayPalProvider({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
